# Скопируйте в .env и подставьте свои значения: cp .env.example .env
# ОБЯЗАТЕЛЬНО: без этих переменных вход в админку отключён (дефолтов нет).
ADMIN_EMAIL=admin@example.com
ADMIN_PASS=change-me
# API_PORT=8787
